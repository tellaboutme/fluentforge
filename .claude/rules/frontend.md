---
paths:
  - "apps/web/**"
  - "packages/contracts/**"
---
- Use Server Components by default; introduce Client Components only for interactivity or browser APIs.
- Keep learning task state explicit and recoverable.
- All controls need labels, focus states, keyboard support, and error messaging.
- Test recording flows with denied permission, missing microphone, cancellation, retry, and upload failure.
- Never calculate authoritative mastery in the browser.
